# Test Results

Generated on 2026-08-13 with:

```bash
python3 -m pytest -q --junitxml=results/pytest.xml
```

Result: **28 passed, 0 failed, 0 skipped**.

The complete machine-readable report is in [`pytest.xml`](pytest.xml).

The comparative benchmark artifacts are generated by:

```bash
python3 -m wam.benchmark --output results --length 360 --trials 10
```

See [`report.md`](report.md) for the data-derived verdict and `plots/` for the figures.

The falsification phase is in [`diagnostics/context_diagnostics.md`](diagnostics/context_diagnostics.md). The original benchmark artifacts remain at the top level; the rerun after diagnostic changes is preserved separately in [`benchmark_after_diagnostics/`](benchmark_after_diagnostics/).

The lead-time experiment is in [`horizon_analysis/report.md`](horizon_analysis/report.md), with separate oracle/WAM horizon, timeliness, compute-gap, DRAM, bandwidth, and failure-breakdown artifacts.

The hardware-realism phase is in [`hardware_feasibility/report.md`](hardware_feasibility/report.md). It preserves the earlier result directories and adds explicit predictor latency/throughput, overlap, pipelining, queue/port pressure, bounded hashed storage, counter quantization, update batching, signature compression, prediction caching, fallback/candidate cost, energy proxy, tolerance, and feasibility-matrix artifacts.

The real-trace phase is in [`real_trace_evaluation/report.md`](real_trace_evaluation/report.md). It contains the source-instrumented fallback results, capture inventory/sanity checks, equal-budget comparative schema, native benchmark/capture workflow references, and explicit limitations separating source instrumentation from binary tracing.

The GMC/WAM complementarity phase is in [`hybrid_analysis/report.md`](hybrid_analysis/report.md). It audits the simplified GMC-style proxy, compares chronological phase windows across all 37 captured workload/seed traces, computes oracle and implementable selector ceilings, records disagreement/features/direct-horizon contribution, and evaluates equal-budget bounded WAM-sidecar splits. Its report applies the requested 2% stopping rule and preserves explicit cap/sampling limitations.
